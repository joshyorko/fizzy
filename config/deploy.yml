<%
require "dotenv"
require "uri"

Dotenv.load(".env.kamal.local") if File.exist?(".env.kamal.local")

kamal_server_alias = ENV.fetch("KAMAL_SERVER_ALIAS")
kamal_public_host = ENV.fetch("KAMAL_PUBLIC_HOST")
kamal_ssh_user = ENV.fetch("KAMAL_SSH_USER")
kamal_storage_path = ENV.fetch("KAMAL_STORAGE_PATH")
kamal_mailer_from_address = ENV.fetch("KAMAL_MAILER_FROM_ADDRESS")
s3_endpoint = ENV.fetch("S3_ENDPOINT")
s3_bucket = ENV.fetch("S3_BUCKET")
s3_region = ENV.fetch("S3_REGION", "auto")
s3_force_path_style = ENV.fetch("S3_FORCE_PATH_STYLE", "false")
s3_bucket_setup_enabled = ENV.fetch("S3_BUCKET_SETUP_ENABLED", "false")
s3_csp_sources = [ s3_endpoint ]

unless s3_force_path_style == "true"
  s3_endpoint_uri = URI.parse(s3_endpoint)
  if !s3_endpoint_uri.scheme.to_s.empty? && !s3_endpoint_uri.host.to_s.empty?
    s3_csp_sources << "#{s3_endpoint_uri.scheme}://#{s3_bucket}.#{s3_endpoint_uri.host}"
  end
end
%>
# Name of this app
service: fizzy
image: joshyorko/fizzy


#-- About your deployment --#

# Where to deploy fizzy
servers:
  web:
    - <%= kamal_server_alias %>

# How you connect to your server
ssh:
  user: <%= kamal_ssh_user %>

# Cloudflare Tunnel handles public TLS; Kamal proxy serves local origin HTTP
proxy:
  host: <%= kamal_public_host %>
  app_port: 80
  ssl_redirect: true
  forward_headers: true
  ssl: false
  run:
    bind_ips:
      - 127.0.0.1

# Your application configuration (secrets come from .kamal/secrets).
env:
  secret:
    - SECRET_KEY_BASE
    - VAPID_PUBLIC_KEY
    - VAPID_PRIVATE_KEY
    - SMTP_USERNAME
    - SMTP_PASSWORD
    - S3_ACCESS_KEY_ID
    - S3_SECRET_ACCESS_KEY
    - AWS_ACCESS_KEY_ID:S3_ACCESS_KEY_ID
    - AWS_SECRET_ACCESS_KEY:S3_SECRET_ACCESS_KEY
  clear:
    BASE_URL: https://<%= kamal_public_host %>
    ASSUME_SSL: true
    FORCE_SSL: true
    MAILER_FROM_ADDRESS: <%= kamal_mailer_from_address %>
    SMTP_ADDRESS: smtp.gmail.com
    MULTI_TENANT: false # Set to true to allow multiple accounts to sign up
    SOLID_QUEUE_IN_PUMA: true # Run background jobs in the app container
    ACTIVE_STORAGE_SERVICE: s3
    S3_ENDPOINT: <%= s3_endpoint %>
    CSP_CONNECT_SRC: <%= s3_csp_sources.uniq.join(" ") %>
    CSP_IMG_SRC: <%= s3_csp_sources.uniq.join(" ") %>
    S3_REGION: <%= s3_region %>
    S3_FORCE_PATH_STYLE: <%= s3_force_path_style %>
    S3_BUCKET_SETUP_ENABLED: <%= s3_bucket_setup_enabled %>
    S3_REQUEST_CHECKSUM_CALCULATION: <%= ENV.fetch("S3_REQUEST_CHECKSUM_CALCULATION", "when_required") %>
    S3_RESPONSE_CHECKSUM_VALIDATION: <%= ENV.fetch("S3_RESPONSE_CHECKSUM_VALIDATION", "when_required") %>
    S3_BUCKET: <%= s3_bucket %>
    AWS_REGION: <%= s3_region %>


accessories:
  cloudflared:
    image: cloudflare/cloudflared:latest
    host: <%= kamal_server_alias %>
    cmd: tunnel --no-autoupdate run
    env:
      secret:
        - TUNNEL_TOKEN:CLOUDFLARED_TOKEN


#-- General configuration --#

registry:
  server: ghcr.io
  username: joshyorko
  password:
    - KAMAL_REGISTRY_PASSWORD

# Handy aliases for interacting with your deployment. For eaxmple: `bin/kamal console` will connect to a
# Rails console in production.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole --include-password"

# Use a persistent host path for sqlite database files and local Active Storage files.
volumes:
  - "<%= kamal_storage_path %>:/rails/storage"

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# Keep builder config available for manual fallback deploys.
builder:
  arch: amd64
